---
- name: loops
  hosts: local
  connection: local
  tasks:
    - name: print names
      ansible.builtin.debug:
        msg: " Hello {{ item }} "
      loop:
        - ramesh
        - suresh
        - praveen
        - yugendar

        # item is a reserve keyword in loops
