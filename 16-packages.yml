---
- name: packages
  hosts: local
  connection: local
  tasks:
    - name: print names
      ansible.builtin.package:
        name: "{{ item }}"
        state: latest
      loops:
        - nginx
        - mysql
        - zip
